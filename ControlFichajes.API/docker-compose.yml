services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: controlfichajes-api
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
    network_mode: host